# 必需：DeepSeek API Key（用于 LLM 对话）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# 必需：OpenAI API Key（用于 Whisper STT + TTS）
OPENAI_API_KEY=sk-your-openai-api-key

# 可选：服务器端口，默认 3000
PORT=3000

# 可选：允许的 origin（CORS），默认 *
ALLOWED_ORIGIN=*
